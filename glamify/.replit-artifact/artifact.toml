kind = "web"
previewPath = "/"
title = "Glamify"
version = "1.0.0"
id = "artifacts/glamify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21606

[services.development]
run = "pnpm --filter @workspace/glamify run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/glamify", "run", "build" ]
publicDir = "artifacts/glamify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21606"
BASE_PATH = "/"
